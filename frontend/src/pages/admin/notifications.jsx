import { EmptyState } from '../../components/UI';

export default function NotificationsPage() {
  return (
    <EmptyState
      title="Notifications"
      description="Notification management is not yet available. Check back later."
    />
  );
}